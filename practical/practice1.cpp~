#include <iostream>
#include <cstring>
#include <cctype>

using namespace std;

int main(){
  //PLEASE PUT YOUR CODE HERE to call the function assigned
  int length = 256;
  char input[length];
  cin.get(input, length);
  cin.get();
  cout << input << endl;
  cout << isspace(input[0]) << endl;
  int i = 0;
  //while (i < length && isspace(input[i] == 0){
  i = 0;
  int first = 0;
  int second = 0;
  while (i < length && input[i] != '\0' && input[i] != '\n'){
  }
  return 0;
}
